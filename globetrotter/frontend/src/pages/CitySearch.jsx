// Placeholder created because the Claude chat lists CitySearch.jsx in the required
// frontend structure, but does not provide an implementation for this page.
// City search is currently implemented inside ItineraryBuilder.jsx.
export default function CitySearch() {
  return (
    <div className="max-w-4xl mx-auto p-6">
      <h1 className="text-3xl mb-4">City Search</h1>
      <p className="text-stone-500">
        City search is currently handled from the Itinerary Builder.
      </p>
    </div>
  );
}